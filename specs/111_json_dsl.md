# JSON DSL

The JSON Module SHALL expose a JSON DSL.

Example capabilities include:

- object creation
- array creation
- filtering
- transformation
- merging
- path lookup

The JSON DSL SHALL compile into native execution plans.

---

