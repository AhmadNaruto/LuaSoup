# Selector Integration

Selector expressions SHALL automatically use Compiled Selectors.

Repeated selectors SHALL reuse cached selector plans.

Applications SHALL never manage selector caches manually.

---

