# Extensibility

New backends SHALL be added without modifying:

- Meta Framework
- Semantic Analyzer
- Intermediate Representation
- Existing Backends

The compiler SHALL remain plugin-oriented internally.

---

