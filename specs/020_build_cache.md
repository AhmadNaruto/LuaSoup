# Build Cache

The compiler SHALL maintain an internal build cache.

The cache MAY store:

- parsed metadata
- semantic models
- IR
- generated descriptors
- documentation cache

Cache invalidation SHALL remain deterministic.

---

