# Generator Architecture

Each generator SHALL consist of:

```
Generator

├── Descriptor Reader

├── Template Engine

├── Validator

├── Formatter

└── Writer
```

Generators SHALL remain independent.

---

