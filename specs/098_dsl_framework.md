# DSL Framework

## Overview

The DSL Framework provides a unified, type-safe, and high-performance Domain Specific Language (DSL) for every module within the Native Runtime Platform.

The DSL SHALL allow applications to express complex operations declaratively instead of imperatively.

The DSL SHALL compile into optimized native execution plans.

Applications SHALL never execute DSL through interpretation whenever compilation is possible.

---

