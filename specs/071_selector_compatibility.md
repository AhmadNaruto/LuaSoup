# Selector Compatibility

Objects supporting selectors SHALL expose a unified selector API.

Supported methods include:

- select()
- selectFirst()
- matches()
- closest()

Selector semantics SHALL remain identical across every supported module.

---

